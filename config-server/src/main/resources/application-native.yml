server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

management:
  endpoints:
    web:
      exposure:
        include: '*'